version: '3.7'
services:

    nginx:
        build: ./nginx
        ports:
            - 80:80
        depends_on:
            - web
        restart: "always"

    web:
        build:
            context: ./server
        container_name: mini_bank_gunicorn
        restart: "always"
        expose:
             - 8000

